# Deprecated root shims — removed

Legacy re-export files (`pipeline.js`, `extraction.js`, `clean.js`, `ocr.js`, `stats.js`) were deleted in the dead-code audit.

Use **`src/core/index.js`** and **`src/core/pipeline/index.js`** instead.
