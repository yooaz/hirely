name: Extraction Release Gate

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  extraction-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Extraction reliability gate (blocks release)
        run: npm test
